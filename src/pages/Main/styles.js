import styled from "styled-components";

export const Title = styled.h1`
  color: #fff;
  /* color: ${(props) => (props.error === true ? "red" : "#7159c1")}; */

  /* small {
    font-size: 14px;
    color: #333;
  } */
`;
